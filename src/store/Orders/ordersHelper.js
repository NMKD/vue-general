export default class Orders {
  constructor(name, phone, key, uid, done) {
    (this.name = name),
      (this.phone = phone),
      (this.key = key),
      (this.uid = uid),
      (this.done = done);
  }
}
